# JWT - JSON Web token - for secure authorization and authentication

## Login end point - generates a JWT

## Protected end point - requires a valid JWT

## Middleware - verifies a JWT

## npm i express dotenv jsonwebtoken

## for HS256

node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"